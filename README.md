# df_du

```
/bin/df -h

# du with ignore mountage
/bin/du -kx /home/centos/ansible/logstash | sort -h

du --help
-k,                   identique à --block-size=1K
-x, --one-file-system    ignorer les répertoires de différents systèmes de
                             fichiers
```
